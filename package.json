{
  "name": "sep2025-project-team_006",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "dev:all": "concurrently \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\"",
    "dev:back": "npm run start:dev --prefix backend",
    "dev:front": "npm run dev --prefix frontend",
    "start:all": "concurrently \"npm start --prefix frontend\" \"npm start --prefix backend\"",
    "build:all": "npm run build --prefix frontend && npm run build --prefix backend",
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "deploy": "npm run build && gh-pages -d out"
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "next": "^15.1.7",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
