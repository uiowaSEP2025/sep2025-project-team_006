// Ignore the warning - needs to be in this format in order for tests to work
// This is also not named conventionally to avoid Next.js from failing to build
module.exports = {
  presets: [
    "@babel/preset-env",
    "@babel/preset-react",
    "@babel/preset-typescript"
  ]
}