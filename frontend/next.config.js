/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    distDir: "out",
    basePath: "/sep2025-project-team_006",
    images: {
        unoptimized: true,
    },
};
module.exports = nextConfig;